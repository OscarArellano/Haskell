{- Realizar un programa o función 
		que permita obtener la media de 3 numeros. -}

{-
import Data.List
import Data.Map (Map)
import qualified Data.Map as M
import Test.QuickCheck

media :: Floating a => [a] -> a
media xs = suma xs / genericLength xs
-}

media3 x y z = (x + y + z) / 3